# Sentinel Cameroon - Production Environment
# Use these settings when deploying to Render

# SQLite database path (automatically initialized by Docker)
DB_PATH=/var/www/html/database/data/sentinel_cameroon.sqlite

# Render deployment indicator (auto-set)
RENDER=true

# PHP Configuration (optional)
ENVIRONMENT=production
DEBUG=false
